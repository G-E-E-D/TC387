# 英飞凌 AURIX TC387 车辆控制工程交接文档

> 文档日期：2026-08-16  
> 工程根目录：`Seekfree_TC387_Opensource_Library`  
> 当前分支：`agent/tc387-vehicle-control`  
> 当前基线提交：`5b4b0b137547b767c6366ef8950ad070884b2a93`（`修正后轮编码器`）  
> 适用硬件：Infineon AURIX TC387QP + 当前车辆控制板 + MT9V03X + MT6701 AB 输出模块 + IMU963RA + IPS200

本文档是按交接时的实际工作区、目标构建产物、主机测试和已有台架数据编写的状态快照。文中严格区分“源码已经实现”“主机算法测试通过”“目标工程编译通过”和“实车已经调通”。这四者不能互相替代。

---

## 0. 接手人必须先读的结论

### 0.1 当前固件是台架测试固件，不是正常整车固件

当前 [`code/vehicle_test_mode.h`](code/vehicle_test_mode.h) 中：

```c
#define VEHICLE_TEMP_TEST_MODE (1U)
```

因此 CPU0 当前只运行六页按键台架测试界面。正常状态机、定位、闭环控制、路径记录、倒车跟踪、UART 命令和 CSV 日志虽然都已写入源码并进入目标工程，但被编译期分支旁路，当前烧录的 ELF/HEX 不会运行这些功能。

恢复正常应用时必须把该宏改为 `0U`，然后在 AURIX Studio 中执行 `Refresh -> Clean -> Build Project`。不要直接使用当前测试固件做自动驾驶。

### 0.2 所有整车标定仍未完成，自动动力输出被锁止

当前 [`code/vehicle_calibration.h`](code/vehicle_calibration.h) 中：

```c
#define CALIBRATION_VALID (false)
```

[`code/vehicle_calibration.c`](code/vehicle_calibration.c) 内所有车辆实测值仍是 `MEASURE_REQUIRED` 占位值，两个转向标定表也是无效的零值占位表。控制层和安全层都检查该开关；保持 `false` 时，正常自动模式的三路执行器输出会被强制为零。

因此，即使关闭台架测试模式，也不能直接运行阶段一或阶段二。必须先完成本文件第 12 节列出的全部标定项。

### 0.3 当前工程不是道路可用版本

当前能证明的是：目标 Debug 工程可编译、台架测试界面可运行、部分硬件链路已验证、纯算法主机测试通过。尚未完成整车闭环、定位、路径记录、倒车、视觉跟车和完整故障注入测试。

此外，启用自动模式前至少要解决以下安全阻断项：

1. 相机/CPU1 停止发布新帧时，最后一帧旧视觉命令可能被 CPU0 反复续期，缺少真正的帧年龄超时。
2. 正常 `CALIBRATION_MODE` 的电机测试在标定无效时无法利用米每秒速度保护，只剩占空比和 2 秒软件超时。
3. CPU 和 Safety 硬件看门狗当前由逐飞时钟初始化关闭；现有 30 ms 软件看门狗依赖 CCU60 中断继续运行，不能覆盖 CPU trap、全局中断锁死等情况。
4. 没有独立急停/驱动 EN 安全链。软件只能令三路 PWM=0、DIR=低。
5. 转向没有上电绝对零位恢复、自动归中或可靠 Z 索引校准。正常应用把第一份有效样本当相对零，所以上电时车轮必须物理居中，或者先在标定模式执行人工置零。

### 0.4 Git 基线不能切错

车辆控制与视觉代码都在：

```text
agent/tc387-vehicle-control
```

远端同名分支与本地同步。`main` 仍停在最初空工程提交 `b9f1dc2`。接手人如果切到 `main`，会看不到本项目绝大多数新增代码。

---

## 1. 一句话回答当前完成度

### 1.1 已经有明确证据能正常工作的部分

| 功能 | 证据 | 结论 |
|---|---|---|
| AURIX Studio Debug 目标构建 | 已有 2026-08-16 15:34:02 的 ELF/HEX/MAP，MAP 含最新车辆、视觉、TIM5 和测试模块 | 目标工程可编译、可生成烧录文件 |
| 六页 IPS 台架测试固件 | 用户已实际看到 IPS 测试页，并通过 K1 切到 MT6701 页面；K2/电机页逻辑已实现 | IPS200、基本按键交互和测试入口可用 |
| MT6701 主计数链 | TIM5 A+DIR 实测；静置 30 s 不漂；左转计数/增量为负，右转为正；`cnt/ph14/T5` 模关系完全一致 | 硬件计数方向、16 位解绕和连续计数基本正确 |
| 纯算法车辆测试 | 2026-08-16 重新构建并执行，26/26 PASS | 被主机测试覆盖的编码器、控制、定位、路径、倒车、安全、诊断算法通过当前测试 |
| 视觉标签纯算法测试 | 合成灰度图测试全部 PASS，CTest 总计 2/2 PASS | 固定 tag36h11 ID0 的识别算法在合成数据上通过 |
| 视觉 DVP 接线/配置 | 车主明确确认 DVP 无问题；当前目标工程已接入 CPU1、VSYNC、DMA 和相机配置 | 可认为硬件线序已确认，但尚不能证明实机识别达到 50 FPS |
| Git 版本状态 | 当前分支与远端同名分支都指向 `5b4b0b1`，写本文档前工作树干净 | 源码基线可追溯 |

### 1.2 已经写完主要代码、但尚未实车调通的部分

- 左右后轮编码器 HAL、方向/速度换算和异常检测。
- IMU963RA 软 I2C 驱动、温度读取、SI 单位转换和 3 秒静态零偏标定。
- 轮速 PI+前馈控制、换向保护、左右轮目标速度分配。
- 转向双回差表插值、PID、死区补偿、软限位和堵转判断。
- 编码器/IMU/转向模型融合定位。
- 阶段一路径记录、距离/时间上限、预处理、重采样、平滑和曲率计算。
- 阶段二倒车索引搜索、反向轨迹跟踪、负速度约束、终点减速和完成判定。
- 状态机、故障锁存、立即停车/受控停车、人工复位和执行器门控。
- UART0 命令解析、STOP 抢占、非阻塞 CSV 环形缓冲日志。
- 正常应用的 IPS 数字仪表。
- MT9V03X 的 CPU1 采集、固定标签识别、跨核快照和视觉横向误差到转向命令的适配。

以上模块的代码和大部分纯算法测试已完成，但没有完成全车端到端实车验收，所有控制增益和安全阈值只能视为初始值。

### 1.3 完全没有写，或只有底层接口没有形成完整功能的部分

以下项目是后续明确开发工作，不是简单填参数：

- 硬件 CPU WDT + SMU 告警到可靠复位 + 复位后执行器安全态的完整实现与台架验证。
- 独立于 MCU 软件的急停/驱动使能安全链。
- 转向上电绝对零位、自动归中、Z 索引硬件捕获、多圈消歧和掉电后零点恢复。
- 标定数据自动采集、拟合、校验、版本号、CRC、DFlash 保存和回滚；当前只能人工修改 C 文件。
- 相机断流/冻结超时故障、真正的硬件帧计数、丢帧/覆盖/撕帧统计。
- 相机双缓冲或明确的 DMA 帧所有权机制。
- 真实相机帧回归数据集、检出率/误检率/最坏处理时间评测。
- 相机内参/畸变标定、真实距离标定和基于距离的减速/停车闭环。
- IPS 实时相机画面及检测框的正式集成。`vision_ips_fast` 已写，但没有调用。
- 运行时自动曝光控制。共享邮箱接口已写，但没有任何调用。
- 完整 app/HAL/多核 HIL 测试、真实故障注入脚本、路测报告和验收数据归档。

以下功能是需求中明确关闭或按设计不实现，不应误判为遗漏：

- GPS：`GPS_ENABLE=0`。
- 磁力计定位融合：`MAG_FUSION_ENABLE=0`。磁数据只做显示/记录。
- 路径掉电保存：`PATH_PERSISTENCE_ENABLE=0`。路径只存 RAM，掉电即丢失。
- 通用 AprilTag/任意 ID/PnP/6DoF：当前算法只识别固定 tag36h11 ID0，这是当前视觉方案的设计边界。

---

## 2. 版本、工具链和构建产物

### 2.1 Git 状态

```text
当前分支：agent/tc387-vehicle-control
HEAD：5b4b0b137547b767c6366ef8950ad070884b2a93
远端：origin/agent/tc387-vehicle-control
远端仓库：git@github.com:G-E-E-D/TC387.git
main：b9f1dc2（初始空工程，不是当前工作分支）
```

写本文档前工作树完全干净，无未提交、未跟踪的源码修改。本文档是本次交接任务新写入的文件，所以交付时 `git status --short` 预期只会看到：

```text
?? 工程交接文档.md
```

这是正常现象。本文档尚未替原负责人自动提交或推送；审核后可单独提交，避免把文档新增误认为源码脏改动。

最近提交：

```text
5b4b0b1 修正后轮编码器
81b1594 修正转向编码器
84254fd 修正编码器引脚
425c7db 新增测试部分代码
cf04c4a 整合视觉识别
9a94ec4 Add bounded stage-one drive diagnostics
74911b3 Implement TC387 path recording and reverse control
b9f1dc2 first commit
```

接手后先执行：

```powershell
git status --short --branch
git branch -vv
git log --oneline --decorate -8
git rev-parse HEAD
```

如果不在正确分支：

```powershell
git switch agent/tc387-vehicle-control
```

复制整个文件夹时要包含隐藏的 `.git` 目录，否则会失去分支、提交历史和远端信息。

### 2.2 工具链

当前已验证的目标构建环境：

```text
Infineon AURIX Development Studio 1.10.28
TASKING VX-toolset for TriCore 1.1r8
目标芯片：TC38x / TC387QP
构建配置：Debug
C 标准：C99
目标优化：-O0，带调试信息
逐飞库：V3.4.3
```

原电脑路径：

```text
C:\Infineon\AURIX-Studio-1.10.28\AURIX-studio.exe
C:\Infineon\AURIX-Studio-1.10.28\tools\Compilers\Tasking_1.1r8\ctc\bin\cctc.exe
```

`.cproject` 中虽然还有 Release 和 External GCC 配置，但它们没有按当前工程完整验证。交接后以 `Debug` 为唯一可信目标基线。

### 2.3 推荐目标构建步骤

1. 用 AURIX Studio GUI 导入工程根目录。
2. 选择 `Debug` 配置。
3. 执行 `Project > Refresh`。
4. 执行 `Project > Clean`。
5. 执行 `Project > Build Project`。
6. 检查 Console 无 error，并确认 ELF/HEX/MAP 时间已更新。

无界面的 `AURIX-studioc` 构建曾受 Project Booster/Workbench 限制，不作为交接后的首选构建方式。

工程移动到新路径后必须 Clean/Build。旧 `Debug` 下的 makefile 和 `.opt` 含原路径 `C:\YX\ADS\Seekfree_TC387_Opensource_Library`，不能在新位置直接复用。

### 2.4 当前可烧录产物

```text
Debug/Seekfree_TC387_Opensource_Library.elf
Debug/Seekfree_TC387_Opensource_Library.hex
Debug/Seekfree_TC387_Opensource_Library.map
```

生成时间：`2026-08-16 15:34:02`。

```text
ELF SHA256 = 385FFC209CF317C705A1E56A9AC8B4F68686EA91C665DA2B5749A9B0756C544A
HEX SHA256 = B9727884E6BA94784775D7E71F84FED25BA05826226192CD0803750AAB7FBC28
```

这些文件对应当前六页台架测试固件。`Debug/` 被 `.gitignore` 忽略，单纯 `git clone` 不会取得这些产物；直接复制整个工程文件夹时才会一并带走。

根目录 `删除临时文件.bat` 会删除 `Debug`。需要保留当前固件时不要运行它。

### 2.5 MAP 内存概况

当前测试固件 MAP 摘要：

```text
总 Code：0x016A04 bytes
总 Data：0x026DFA bytes
CPU0 DSPR data：0x006193，空闲 0x03226D
CPU1 DSPR data：0x01C681，空闲 0x01CD7F
CPU1 PSRAM code：0x002BC8，空闲 0x00D438
```

CPU1 DSPR 的主要占用是视觉积分图、连通域栈、visited 位图和 188×120 私有帧副本。目前还有余量，但继续加入大数组时必须检查 MAP，不能只看链接是否成功。

---

## 3. 工程入口、多核和中断结构

### 3.1 CPU0

活跃入口是 [`user/cpu0_main.c`](user/cpu0_main.c)：

```text
clock_init()
debug_init()
vehicle_app_init()
cpu_wait_event_ready()
while(1) vehicle_app_process()
```

测试模式为 1 时，`vehicle_app_init/process` 直接转到 `vehicle_test_mode_init/process`。

正常模式为 0 时，CPU0 执行传感器、定位、跟踪、控制、安全、日志、显示和 UART 命令调度。

### 3.2 CPU1

活跃入口是 [`user/cpu1_main_vision.c`](user/cpu1_main_vision.c)，负责：

1. 等待 CPU0 完成相机与共享区初始化。
2. 从 MT9V03X DMA 缓冲复制一帧 188×120 灰度图到 CPU1 DSPR。
3. 运行固定 tag36h11 ID0 识别。
4. 记录单帧处理耗时。
5. 通过 `vision_shared` 向 CPU0 发布快照。

旧 [`user/cpu1_main.c`](user/cpu1_main.c) 被 `.cproject` 排除，不是活跃入口。

### 3.3 CPU2/CPU3

CPU2 和 CPU3 保留逐飞空模板，只参与启动同步，没有业务任务。这是当前设计状态，不要误以为丢了两个源文件。

### 3.4 活跃中断文件

实际编译的是 [`user/isr_vision.c`](user/isr_vision.c)：

- CCU60 CH0：1 ms tick，调用 `vehicle_hal_timer_tick_isr()`。
- ERU CH3 / P02.0：MT9V03X VSYNC，调用 `camera_vsync_handler()`。
- DMA CH5：相机 DMA 完成，调用 `camera_dma_handler()`。
- UART0 RX：调试命令接收。
- UART1 RX：相机配置通信。

旧 [`user/isr.c`](user/isr.c) 被 `.cproject` 排除。不要同时启用两个 ISR 文件，否则会产生重复中断符号或丢失相机回调。

`.cproject` 还排除了 `host_tests` 和 `tmp`，所以主机测试的 `main()` 不会进入 TC387 目标构建。

### 3.5 正常模式调度周期

| 任务 | 周期 |
|---|---:|
| fast tick / 编码器捕获 | 1 ms |
| 估计器 | 5 ms |
| 控制器 | 5 ms |
| 安全监控 | 5 ms |
| 倒车 tracker / 阶段一视觉命令 | 10 ms |
| CSV 日志 | 20 ms |
| IPS 数字显示 | 100 ms |

其他超时：

```text
视觉/外部命令 deadman：150 ms
控制 deadline：30 ms
传感器 freshness：50 ms
受控停车最大等待：3 s
```

这是协作式主循环，不是 RTOS。当前没有任务执行时间统计、周期抖动统计或调度超限日志。

---

## 4. 硬件映射和当前验证状态

应用级引脚以 [`code/vehicle_hardware_config.h`](code/vehicle_hardware_config.h) 为准；原理图以根目录 [`SCH_Schematic1_1_2026-08-09.pdf`](SCH_Schematic1_1_2026-08-09.pdf) 为准。

| 功能 | 当前引脚/外设 | 状态 |
|---|---|---|
| 左后轮电机 PWM | ATOM0 CH2 / P21.4，20 kHz | 驱动已写，电机方向/起动占空比未测 |
| 左后轮电机 DIR | P21.5 | 高低电平对应前进方向未测 |
| 右后轮电机 PWM | ATOM0 CH1 / P21.3，20 kHz | 驱动已写，电机方向/起动占空比未测 |
| 右后轮电机 DIR | P21.2 | 高低电平对应前进方向未测 |
| 转向电机 PWM | ATOM0 CH4 / P02.4，20 kHz | 驱动已写，闭环未调 |
| 转向电机 DIR | P02.5 | `S+` 对应左/右尚未提交结果 |
| 左后轮编码器 A | TIM6 / P20.3 | 已改成 A+DIR 硬件计数，需重新实测 |
| 左后轮编码器 DIR | TIM6 / P20.0 | 已配置，正向符号未测 |
| 右后轮编码器 A | TIM3 / P02.6 | 已改成 A+DIR 硬件计数，需重新实测 |
| 右后轮编码器 DIR | TIM3 / P02.7 | 已配置，正向符号未测 |
| MT6701 A | TIM5 / P10.3 | 主位置计数，已实测 |
| MT6701 DIR | TIM5 / P10.1 | 硬件方向输入，已实测左负右正 |
| MT6701 B | GPIO P10.2 | 仅 1 ms 软件相位诊断，不参与位置 |
| MT6701 Z | GPIO P10.5 | 仅 1 ms 索引统计，不清零、不纠偏 |
| K1 | P20.6，低有效 | 实际切页可用 |
| K2 | P20.7，低有效 | 停止/步进逻辑已用 |
| IMU963RA | 软 I2C：SCL P20.11，SDA P20.12 | 驱动已改为两线接口；实物轴向/动态未标定 |
| IPS200 | SPI2：SCLK P15.3、MOSI P15.5、CS P15.2、RST P15.1、DC P15.0、BL P15.4 | IPS 测试页已实际显示 |
| MT9V03X D0..D7 | P00.0..P00.7 | DVP 线序由车主确认 |
| MT9V03X PCLK | P02.1 / ERU CH2 / DMA CH5 | 相机 DMA 专用资源 |
| MT9V03X VSYNC | P02.0 / ERU CH3 | 相机 VSYNC 中断专用资源 |
| MT9V03X SCCB/UART1 | P02.3/P02.2 | 配置接口，SCCB 失败时回退 UART1 9600 |
| UART0 调试 | TX P14.0、RX P14.1，460800 8N1 | 正常应用命令/CSV 通道；如插装 CI1302 座需复核冲突 |

注意事项：

- P10.2/P10.3 的 ERU CH2/CH3 已分别被相机 PCLK/VSYNC 资源占用，不能为了 MT6701 B/A 双边沿解码重配 ERU，否则会破坏 DVP 相机。
- P10.5 在根目录“不推荐引脚”资料中属于启动相关风险脚，但原理图已经把 MT6701 Z 接到它。上电时需确认外部模块不会强驱动出危险启动电平。
- 当前 DRV8701 链路只有 PH/DIR 与 EN/PWM，没有独立应用级 EN 或急停 GPIO。安全停止只能软件写 PWM=0、DIR=低。
- HAL 中电机、后轮编码器、转向传感器、timer 的 ready 多数是在初始化函数调用后直接置真，不代表线路或传感器真的有脉冲。只有 IMU 和相机 ready 包含设备初始化返回值。

---

## 5. 当前六页台架测试固件

源码：[`code/vehicle_test_mode.c`](code/vehicle_test_mode.c)。

### 5.1 页面和操作

1. `TEST 1/6`：HAL 总览、相机配置、IMU/IPS 状态。
2. `TEST 2/6`：左右后轮编码器 raw 和每 20 ms delta。
3. `TEST 3/6`：MT6701 `ph14/cnt/T5/d`、A/B/Z/DIR、电平和 Z 统计。
4. `TEST 4/6`：IMU 加速度、角速度、磁场、温度原始值和通信状态。
5. `TEST 5/6`：视觉 READ、SEQ、FPS、识别结果、置信度、灰度统计和处理时间。
6. `TEST 6/6`：左/右/转向三个电机的正负方向低占空比测试。

普通页面：

- K1：下一页。
- K2：立即写三路 PWM=0。

电机页面：

- K1+K2 同时按住 2 秒才解锁。
- 解锁后 K2 循环：`L+ -> L- -> R+ -> R- -> S+ -> S- -> 0`。
- 固定占空比 8%。
- 每一步最长 2 秒，主循环正常时自动停。
- K1 撤销解锁并停机。
- 必须架空车辆、有人盯守并准备断电。

### 5.2 测试模式已知限制

- 当前第 6 页未解锁时 K1 也不能返回第 1 页，通常需重启退出该页。
- 2 秒停止依赖 CPU0 主循环继续运行；测试模式没有启用正常应用的 30 ms control heartbeat，CPU 卡死时不能保证自动停 PWM。
- `M/E/S:Y` 只表示 HAL 初始化流程走完，不是硬件验收结果。
- MT 页的 `ph14` 是 `cnt & 0x3FFF`，不是独立的 SSI 绝对角。
- MT 页的 `SW:x/y` 是 1 ms GPIO 轮询诊断，快速运动时会漏 B/Z 边沿，不能用作位置或安全判据。
- 视觉页只显示数字，不显示相机图像和检测框。

---

## 6. MT6701 当前状态和最新实测

### 6.1 接口定义

实物模块从 Z 到 DIR 的四根信号映射为：

```text
Z   -> P10.5
B   -> P10.2
A   -> P10.3
DIR -> P10.1
```

主位置源是 `TIM5 A=P10.3 + DIR=P10.1`：

- `T5`：16 位 GPT12 原始计数。
- `d`：最近一次捕获的 T5 模差。
- `cnt`：对 T5 进行 16 位解绕后的 `int64_t` 连续计数。
- `ph14`：`cnt mod 16384`，供现有 `vehicle_mt6701` 通用 14 位接口使用。
- B：软件诊断。
- Z：索引次数/相邻索引距离诊断；当前不改变计数。

Z 并不会提高单个 A 脉冲的分辨率。要让 Z 提高长期可靠性，必须另做硬件捕获、确定 Z 到车辆中位的偏置、处理多圈消歧并定义开机未经过 Z 的策略；当前这些都没有实现。

### 6.2 已确认结果

- 上电物理居中时初始计数约 0。
- 静置 30 秒计数不增加。
- 物理右转时 `d/cnt` 为正。
- 物理左转时 `d/cnt` 为负。
- 最新 9 组数据中 `ph14 = cnt mod 16384`、`T5 = cnt mod 65536` 全部一致。

最新两轮顺序为“居中 -> 左打死 -> 回中 -> 右打死 -> 回中”：

| 序号 | 位置 | cnt | ph14 | T5 |
|---:|---|---:|---:|---:|
| 1 | 初始中位 | 1 | 1 | 1 |
| 2 | 左打死 1 | -24750 | 8018 | 40786 |
| 3 | 回中 | 694 | 694 | 694 |
| 4 | 右打死 1 | 24408 | 8024 | 24408 |
| 5 | 回中 | 9 | 9 | 9 |
| 6 | 左打死 2 | -25582 | 7186 | 39954 |
| 7 | 回中 | 2 | 2 | 2 |
| 8 | 右打死 2 | 24250 | 7866 | 24250 |
| 9 | 回中 | 6 | 6 | 6 |

### 6.3 为什么还不能写正式软限位

- 两次左端相差 832 count，约为均值绝对值的 3.31%。
- 两次右端相差 158 count，约为均值的 0.65%。
- 第一次左转回中为 +694；后续回中为 +9、+2、+6。

这说明计数链本身已经自洽，但机械中位复现、触碰止挡方式、回差或计数重复性仍未完成验收。现在不能把端点直接写入正式标定，更不能将 `CALIBRATION_VALID` 改为 `true`。

曾提出的台架保护候选：

```text
左软限位约 -23500
右软限位约 +23000
```

它们只是相对机械止挡内缩后的候选值，必须在 5～10 轮重复测试合格后再决定是否使用。

### 6.4 下一轮 MT6701 验收方法

1. 给真实机械中位做固定标记，不要凭目测回中。
2. 每次从同一中位上电/执行置零。
3. 统一慢速、统一止挡接触力度，连续做 5～10 轮：左端、回中、右端、回中。
4. 每个位置记录 `cnt`，运动时观察 `d` 符号，静止后应为 0。
5. 暂定验收门槛：同一端点极差不超过约 50 count，回中绝对误差不超过约 20 count；最终应按允许车轮角误差反推计数门槛。
6. 如果仍有 100～800 count 的差异，先查机械回差、止挡弹性、安装松动、A/DIR 波形，不要用软件平均值掩盖。
7. 明确产品启动零点方案：强制居中上电、人工 `STEER ZERO`，或新增可靠 homing/Z 方案。

### 6.5 转向标定符号约定

工程定义“物理左转角为正，物理右转角为负”。实测 count 则是左负右正。因此：

```text
负 count（左转） -> 正 steering angle
0 count（中位）  -> 0 rad
正 count（右转） -> 负 steering angle
```

每张表必须按 `continuous_count` 严格递增排列，所以角度必须严格单调递减。两张回差表都要遵守同一方向。

---

## 7. 视觉系统当前状态

### 7.1 已实现的数据链

```text
MT9V03X DVP/DMA
    -> CPU1 复制 188x120 灰度帧
    -> 固定 tag36h11 ID0 识别
    -> vision_shared 跨核快照
    -> CPU0 TEST 5/6 数字显示
    -> 正常模式 perception
    -> 固定 0.15 m/s + 最大 0.35 rad 的 Stage1 命令
```

视觉结果包含：detected/valid/predicted、中心坐标、像素/Q15 横向误差、尺寸、置信度、丢帧数、bbox、四角、透视标志、灰度分位、暗/饱和比例、标签对比度以及可选距离。

目标在图像右侧时 `error_x` 为正；车辆约定正转角为左，因此感知适配层输出相反符号的转向命令。

### 7.2 相机配置

```text
分辨率：188 x 120
目标配置帧率：50 FPS
自动曝光：关闭
固定曝光：512
增益：32
PCLK 模式：0
DMA channel：5
```

配置值不等于实机已经达到该帧率。需要以 TEST 5/6 的 `SEQ/FPSx10/PROC` 实测为准。

### 7.3 已验证范围

纯算法合成图测试已覆盖：

- 两帧锁定、中心/左右误差符号。
- 移动保持、短时预测、长期丢失清零和重新捕获。
- 实心黑块和错误 6×6 码字拒绝。
- 3:4 压缩、透视斜视、四角提取、90° 旋转。
- 高亮梯度、低对比度拒绝、灰度/饱和统计。
- 单点距离函数和非法输入清空。

这些只是合成灰度图测试，不覆盖真实 DMA、跨核、光照、运动模糊和复杂背景。

### 7.4 未调通和已知缺陷

1. 尚无 TEST 5/6 的真实 `SEQ/FPSx10/PROC` 记录，不能宣称 50 FPS 识别通过。
2. 尚无真实标签的近/中/远、斜视、逆光、户外、快速横移、错误标签误检测试。
3. 距离函数虽已写且主机测试通过，但 CPU1 明确把距离 scale/near/far 设为 0，运行时距离功能关闭；当前车速始终是固定 0.15 m/s。
4. `vision_ips_fast` 已写灰度图和框叠加，但全工程没有调用，链接器将入口列为 unreferenced。当前 IPS 只显示数字。
5. 曝光请求/状态邮箱已写但没有调用，没有运行时曝光控制。
6. 算法只认 tag36h11 ID0，不是完整 AprilTag3，不支持任意 ID 或 6DoF。

### 7.5 启用视觉行驶前必须修复

#### P0：旧帧命令可能永不过期

`perception_update()` 当前不检查 `camera_sequence` 是否变化，也没有帧时间戳。每次 tracker 周期读到最后一份仍为 valid 的快照，都会重新发布同一条命令并刷新 150 ms deadman。若相机、DMA 或 CPU1 在最后一帧有效后冻结，车辆可能继续按旧命令以 0.15 m/s 行驶。

最低修复要求：

- 仅当 `camera_sequence` 增加时更新命令。
- 保存最后新帧时间。
- 超过 60～100 ms 没有新帧时立即 invalidate，目标速度变 0，并上报 camera stale fault。
- 最好把采集/发布时间戳加入共享快照。

#### P0：单 DMA 缓冲存在覆盖/撕帧窗口

CPU1 在 finish flag 后 memcpy 源图像，但下一次 VSYNC 会重新启用 DMA；如果 CPU1 落后，可能在 DMA 写源缓冲时复制。应先测最坏 `PROC`，随后采用 ping-pong DMA 或明确的缓冲所有权/丢帧策略。

#### P1：DMA 恢复边沿不一致

相机初始 DMA 使用 PCLK 上升沿；库的 transaction-request-lost 恢复路径在下一次 VSYNC 重新配置时使用下降沿。需要统一到实测正确边沿，并专门制造 DMA 丢请求验证恢复。

### 7.6 视觉台架验收

1. 保持测试模式和 PWM 安全。
2. TEST 1/6 确认 `CAM:Y`。
3. TEST 5/6 静置 30 秒：`READ:Y`，`SEQ` 持续增加，`FPSx10` 应约为 500。
4. 增加 `process_us_max` 显示/日志；逐帧处理要求最坏处理时间小于 20 ms，并留有裕量。
5. 打印正确 tag36h11 ID0：连续两帧后 D=1、V=1、P=0、CONF≥650。
6. 标签右移时 EX/Q 为正，左移时为负。
7. 遮挡时算法可能短时预测，但控制层配置 `VEHICLE_VISION_MAX_LOST_FRAMES=0`，第一份 predicted 结果就不应继续驱动车辆。
8. 修复 stale 后拔相机/停 PCLK/VSYNC，100 ms 内目标速度必须变 0。
9. 保存原始 188×120 帧，建立可重复的离线回归集。

---

## 8. 正常车辆应用中已经实现的模块

以下模块是“代码存在且主要算法已完成”，不是“实车已调通”。

### 8.1 `vehicle_encoder`

- 16 位原始计数模差解绕到 `int64_t`。
- 正向符号、米每计数、时间戳和速度计算。
- 非递增时间、跳变/超速无效样本处理。
- freshness 判断。

主机测试覆盖正反转、16 位回卷和无效样本到安全故障。

### 8.2 `vehicle_mt6701`

- 14 位角解绕、多圈连续计数、相对零。
- SSI 24 位解析、CRC6 和磁场状态校验仍保留，可供其他硬件复用。
- 实际车辆走 AB/TIM5 路径，HAL 将连续计数的低 14 位送入通用解绕层。

实际 AB 模式没有利用 Z 恢复绝对位置。

### 8.3 `vehicle_imu`

- 轴映射/符号和原始量到 SI 单位转换。
- 温度转换。
- 连续静止 3 秒且至少 400 样本后计算三轴陀螺零偏和噪声。
- 运动会重启静态标定窗口。
- 静止/运动 bias 更新和 freshness。

该模块没有进入当前主机 CMake 测试，实际轴向、安装位置和动态表现未验证。HAL 的通信健康采用“acc/gyro/temp 连续完全不变约 55 ms”作为冻结启发式，不是真正的 I2C 错误返回/DRDY 监测。

### 8.4 `vehicle_localization`

- 左右轮有符号里程/速度。
- IMU yaw rate、轮速差 yaw rate、转向模型 yaw rate 的自适应融合。
- 连续 yaw，不在 ±π 处截断。
- 倒车保持负位移/负速度。
- 静止 gyro bias 修正、轮滑和模型分歧诊断。

仅在合成直线和定曲率数据上测试；没有实车地面真值对比。

### 8.5 `vehicle_control`

后轮：

- PI + 前馈。
- 积分限幅、输出限幅、斜率限制。
- 正反换向先归零并驻留。
- 基于中心速度/曲率的左右轮目标分配。

转向：

- 两张 `count -> equivalent steering angle` 回差表。
- 根据物理运动方向选择 approach table。
- PID、积分限幅、输出斜率、位置死区。
- 左右起动死区补偿。
- 机械软限位：只禁止继续向外，允许向内退出。
- 堵转检测。

主机测试覆盖插值、两种表斜率、软限位方向和回差表切换；增益没有实车调。

### 8.6 `vehicle_path`

- 单一静态缓冲 `4096 * 24 = 98304 bytes`，无 malloc。
- 阶段一按弧长/转角采样。
- 100 m/120 s 上限。
- 去重、坏段检测、yaw 解绕、长段加密。
- 3 cm 重采样、滑窗平滑、曲率计算和最终校验。
- 容量/溢出诊断。

路径只在 RAM；复位或断电后丢失，这是需求设定。

### 8.7 `vehicle_reverse_tracker`

- 从路径末端向起点反向遍历，进度索引只减不增。
- 倒车 travel yaw 使用 body yaw + π。
- 曲率前馈 + 横向误差 + 航向误差反馈。
- lookahead 自适应、曲率/误差降速。
- 目标速度严格限制在 `[-2, 0] m/s`。
- 终点减速、近闭合路径进度门控、索引重捕获/丢失诊断。

主机测试有 S 弯、左右弯、近闭合路径和反馈符号；没有实车倒车。

### 8.8 `vehicle_state_machine`

状态：

```text
BOOT
SENSOR_CALIBRATION
IDLE
STAGE1_RECORD
STAGE1_FINISHED
STAGE2_REVERSE
FINISHED
FAULT
CALIBRATION_MODE
```

故障优先进入 FAULT；阶段一和阶段二需要有效标定；路径预处理失败不能进入阶段二。

### 8.9 `vehicle_fault` / `vehicle_safety`

已有 20 个 fault bit：驱动初始化、左右编码器跳变/堵转、轮差、MT 通信/跳变、转向限位/堵转、IMU 通信/范围/超时、控制超时、路径容量/有效性、索引丢失、跟踪误差、numeric、日志溢出。

支持：

- active 与 latched 分离。
- 严重故障立即 PWM=0。
- 轮差、路径/跟踪等非立即故障请求受控减速，停稳或超时后转 FAULT。
- 条件消失后仍需人工复位。
- 标定无效时执行器门控为零。

未完成真正的硬件看门狗和独立安全链。

### 8.10 `vehicle_diagnostics` / `vehicle_log` / `vehicle_display`

- UART0 命令不区分大小写。
- STOP 可以覆盖尚未消费的 DRIVE；全局 STOP 不会被后到命令降级。
- CSV 使用 16 KiB 静态环形缓冲，每轮限额发送，避免整行阻塞控制环。
- CSV 50 Hz，29 列。
- 正常应用 IPS 有四页数字状态界面。

当前测试模式下这些正常应用功能不运行；UART 解析只有主机测试，没有板级整链结果。

### 8.11 `vehicle_app`

已完成正常模式的初始化、周期调度、状态入口、传感器/定位/控制/安全任务、受控停车、执行器二次门控、UART 命令、CSV 和 IPS 调度。

但整个 app/HAL/多核整链没有自动集成测试，也没有完成实车阶段一/阶段二流程。

---

## 9. 主机测试状态

### 9.1 本次重新验证结果

2026-08-16 使用 MinGW GCC 15.1 在工程外临时目录重新配置、编译并运行：

```text
CTest：2/2 passed
vehicle_host_tests：26/26 passed
vision_tag_tracker_host_tests：PASS all vision tracker tests
```

车辆 26 项：

```text
01 encoder forward/reverse
02 encoder 16-bit overflow
03 MT6701 zero crossing
04 MT6701 multi-turn return to zero
05 steering interpolation/limits
06 straight localization forward/reverse
07 fixed-curvature localization forward/reverse
08 path static capacity
09 yaw crossing +/-pi
10 path resampling
11 reverse path index traversal
12 reverse tracker straight steering sign
13 reverse tracker left-curve steering sign
14 reverse tracker right-curve steering sign
15 sensor fault transitions to FAULT
16 CALIBRATION_VALID=false zero output
17 MT6701 SSI CRC/magnetic rejection
18 online path capacity boundary
19 steering soft-limit direction gate
20 S-curve reverse traversal/feedback
21 near-closed path finish/progress gate
22 invalid encoder immediate safety fault
23 steering approach hysteresis both slopes
24 wheel mismatch controlled-stop routing
25 diagnostic DRIVE parsing/queue
26 diagnostic STOP preemption
```

### 9.2 测试命令

如果接手电脑的 `cmake/gcc` 已在 PATH：

```powershell
cmake -S host_tests -B host_tests/build
cmake --build host_tests/build --parallel 4
ctest --test-dir host_tests/build --output-on-failure
```

本机实测命令使用：

```powershell
cmake -S host_tests -B <工程外构建目录> -G "MinGW Makefiles" `
  -DCMAKE_C_COMPILER=C:/YX/mingw64/bin/gcc.exe `
  -DCMAKE_MAKE_PROGRAM=C:/YX/mingw64/bin/mingw32-make.exe
cmake --build <工程外构建目录> --parallel 4
ctest --test-dir <工程外构建目录> --output-on-failure
```

### 9.3 主机测试没有覆盖什么

`host_tests/CMakeLists.txt` 没有 host 编译：

- `vehicle_app.c`
- `vehicle_hal.c`
- `vehicle_imu.c`
- `vehicle_log.c`
- `vehicle_display.c`
- `vehicle_perception.c`
- `vehicle_vision_camera.c`
- `vision_shared.c`
- TC387 GPT12/PWM/I2C/SPI/DMA/ERU/多核代码

所以 26/26 和视觉 PASS 只能证明被测纯算法，不代表目标硬件、时序或整车闭环通过。

---

## 10. UART0 诊断和日志

正常模式串口：

```text
UART0，460800，8N1
TX=P14.0，RX=P14.1
```

命令：

```text
HELP
LOG START
LOG STOP
ENC ZERO
STEER ZERO
STEER LIMIT LEFT
STEER LIMIT RIGHT
MOTOR LEFT <duty>
MOTOR RIGHT <duty>
MOTOR STEER <duty>
STAGE1 START
DRIVE <speed_mps> <steering_rad>
STAGE1 STOP
STAGE2 START
STOP
CONFIG
FAULT RESET
CAL MODE
IDLE
```

限制：

- `MOTOR ...` 原始占空比范围 `[-0.12, +0.12]`，2 秒超时。
- `DRIVE` 只允许安全的 `STAGE1_RECORD`，范围 `[0,0.30] m/s` 和 `[-0.35,+0.35] rad`，需在 150 ms 内持续刷新。
- `STOP` 对 pending DRIVE 有抢占优先级。
- 当前测试模式下正常诊断处理主循环被旁路，不能按以上命令控制；需先关闭测试模式。

CSV 字段：

```text
timestamp_us,state,left_encoder_count,right_encoder_count,
left_speed_mps,right_speed_mps,mt6701_raw,steering_continuous_count,
steering_angle_rad,ax,ay,az,gx,gy,gz,imu_temperature,
pose_x,pose_y,pose_yaw,fused_speed,target_speed,target_steering,
left_pwm,right_pwm,steering_pwm,path_index,cross_track_error,
heading_error,fault_flags
```

---

## 11. 当前固定车辆/任务参数

[`code/vehicle_config.h`](code/vehicle_config.h) 中已确定：

```text
轴距：0.680 m
前轮距：0.590 m
后轮距：0.585 m
阶段一路径距离上限：100 m
阶段一路径时间上限：120 s
倒车全局最大速度：2.0 m/s
路径缓冲：4096 点，98304 bytes
记录间距：0.025 m
重采样间距：0.030 m
GPS：关闭
磁力计融合：关闭
路径持久化：关闭
视觉目标速度初值：0.15 m/s
视觉转角上限：0.35 rad
视觉最低置信度：650/1000
控制不接受 predicted 帧：MAX_LOST_FRAMES=0
```

轮速 PI、转向 PID、安全阈值、倒车 lookahead/反馈/速度曲线虽然有数值，但尚未实车调参，不要把它们当最终参数。

---

## 12. 必须完成的全部标定

标定只允许写入 [`code/vehicle_calibration.c`](code/vehicle_calibration.c) 和最终开关 [`code/vehicle_calibration.h`](code/vehicle_calibration.h)。

### 12.1 后轮编码器

- `left_encoder_forward_sign`：车辆前进时左计数应乘的 ±1。
- `right_encoder_forward_sign`：车辆前进时右计数应乘的 ±1。
- `left_meter_per_count`。
- `right_meter_per_count`。
- `left_counts_per_wheel_rev`。
- `right_counts_per_wheel_rev`。

建议同时做“悬空整圈”和“地面已知距离”两种测量。米每计数最终应优先反映装车后有效滚动周长，并记录轮胎压力/载荷。

### 12.2 后轮电机

- `left_motor_forward_direction`：raw DIR 高或低哪个是车辆前进。
- `right_motor_forward_direction`。
- `left_motor_start_duty_forward`。
- `left_motor_start_duty_reverse`。
- `right_motor_start_duty_forward`。
- `right_motor_start_duty_reverse`。

四个起动值要分别测，因为正反和左右可能不同。它们必须不大于轮速控制输出上限 0.80。

### 12.3 转向电机和软限位

- `steering_left_direction`：正控制输出要求物理左转时，原始 DIR 应为高还是低。
- `steering_start_duty_left`。
- `steering_start_duty_right`。
- `steering_left_soft_limit_count`。
- `steering_right_soft_limit_count`。

测试页的 `S+` 表示 P02.5=高：

- 若 `S+` 使车轮物理左转、MT count 减小，则 `steering_left_direction=+1`。
- 若 `S+` 使车轮物理右转、MT count 增大，则 `steering_left_direction=-1`。

两个转向起动值必须不大于控制输出上限 0.55。软限位应内缩于机械止挡，不能直接用撞到止挡的计数。

### 12.4 两张转向角度回差表

需要从左侧接近和从右侧接近各采一张：

```c
const SteeringCalibrationPoint g_steering_calibration_from_left[] =
{
    { count_0, equivalent_front_wheel_angle_rad_0 },
    { count_1, equivalent_front_wheel_angle_rad_1 },
    /* 建议至少 8 点，最多 24 点 */
};

const SteeringCalibrationPoint g_steering_calibration_from_right[] =
{
    { count_0, equivalent_front_wheel_angle_rad_0 },
    { count_1, equivalent_front_wheel_angle_rad_1 },
};
```

角度必须是车辆等效自行车模型前轮角，不是电机轴角或 MT6701 磁铁角。建议用转盘/角度尺测真实车轮方向，并记录左右轮后换算等效角。

当前符号要求：count 严格递增、angle 严格递减；左端负 count 对应正角，右端正 count 对应负角。

### 12.5 IMU

- `imu_position_x_m/y_m/z_m`：相对车辆坐标原点的安装位置。
- `imu_axis_map[3]`：传感器 XYZ 到车体 XYZ 的排列。
- `imu_axis_sign[3]`：每个车体轴的 ±1。

当前 `{0,1,2}` 和 `{1,1,1}` 也标记为 `MEASURE_REQUIRED`，不能因为数值看起来合理就当成确认值。

### 12.6 动力学安全边界

- `max_acceleration_mps2`。
- `max_deceleration_mps2`，表示正常受控停车能力，不是碰撞急停峰值。
- `max_lateral_acceleration_mps2`。

应在低速、有人监护和可立即断电条件下逐步测量，保留安全裕量。

### 12.7 打开标定的顺序

1. 填完所有值和两张表。
2. 仍保持 `CALIBRATION_VALID=false`。
3. 运行主机测试。
4. 人工检查符号、量纲、软限位、表单调性和起动占空比上限。
5. 关闭测试模式但保持标定 false，先观察输入、状态和零输出。
6. 通过代码审查后，最后一步才改 `CALIBRATION_VALID=true`。
7. 重新 Clean/Build，架空车辆测试所有安全门和故障。

---

## 13. 已知缺陷和风险清单

### 13.1 自动模式启用前必须处理

| 优先级 | 问题 | 后果 | 建议 |
|---|---|---|---|
| P0 | 视觉旧帧可反复续期 | 相机/CPU1 冻结后可能继续行驶 | 用 sequence+时间戳做 stale fault，60～100 ms 无新帧即目标速度 0 |
| P0 | 无硬件 WDT/SMU 安全复位 | CPU trap/关中断时 PWM 可能保持 | 设计 WDT timeout、SMU alarm→reset、RSTCON，断开调试器做失喂狗实测 |
| P0 | 无独立急停/EN | MCU 单点故障无法硬切动力 | 增加硬件急停/驱动使能链，至少台架保留人工断电 |
| P0 | 上电转向零位不可靠 | 未居中上电会使软限位和转角整体偏移 | 制定强制居中/人工 ZERO，或实现 homing/Z 绝对参考 |
| P0 | 未标定 CAL 电机测试速度保护失效 | 测试轮可能超速 | 用原始计数变化直接限速/限位，不依赖有效米每计数 |
| P0 | 所有车辆标定为空 | 闭环符号、尺度和限位未知 | 完成第 12 节后才允许自动输出 |

### 13.2 高优先级工程问题

- 相机源为单缓冲，下一帧 DMA 与 CPU1 memcpy 可能并发，需双缓冲或所有权机制。
- 相机 DMA 初始用 PCLK 上升沿，错误恢复路径用下降沿，恢复行为不一致。
- `vehicle_app_commissioning_ready()` 没有检查 `steering_sensor_ready`，而 `critical_ready` 检查了它；两个 readiness 定义应统一。
- HAL 多项 ready 是“配置函数执行过”而不是硬件自检通过，需要真实脉冲/反馈健康检查。
- 后轮 TIM6/TIM3 目前每次 `encoder_get_count()` 后 `encoder_clear_count()`；读取与清零之间理论上有丢 A 边沿窗口。若实测累计误差，改为类似 TIM5 的自由运行 snapshot 模差。
- fast tick catch-up 条件当前可能在达到 catch-up 上限时多取走一个 tick；应调整判断顺序并记录 overrun。
- B/Z 只按 1 ms 轮询；快速运动时诊断计数不可靠，不能转成 fault。
- IMU 通信健康只是“原始值冻结”启发式，不是总线 ACK/DRDY/超时的完整诊断。
- 调试 UART0 引脚与原理图 CI1302 模块座存在条件性复用风险，插装前复核。

### 13.3 一般问题和维护提醒

- 电机测试页当前无法用 K1 正常循环回第一页。
- `vision_ips_fast` 和曝光邮箱接口为未调用代码。
- 视觉文件头仍有少量“TC377”历史字样，实际目标是 TC387；不影响编译但应清理文档。
- `Debug` 内可能保留旧生成子目录；以 `.cproject` 和 Clean 后的 `Debug/sources.mk` 为准，不要把生成目录当源码。
- `tmp/pdfs/schematic` 是原理图渲染图片，权威源仍是根目录 PDF；`tmp` 被 Git 忽略，可重建。
- 原工程部分中文注释使用 GBK；新车辆源码主要是 ASCII。修改旧中文文件时注意 ADS 编码配置，避免整文件乱码 diff。

---

## 14. 推荐后续工作顺序和验收门槛

下面顺序不要跨越。每一级未通过时，不进入更高动力、更高速度或更复杂闭环。

### 阶段 0：接收和复现

- 核对分支、HEAD 和工作区状态。
- 备份当前测试 ELF/HEX，并核对 SHA256。
- 在新电脑 GUI Clean/Build，重新生成测试固件。
- 烧录后确认 TEST 1/6、K1/K2、IPS 正常。

完成标准：新电脑能从源码独立生成并烧录相同功能的测试固件。

### 阶段 1：所有输入设备台架验收

#### 后轮编码器

- 两轮分别正转/反转，记录 delta 符号。
- 每轮至少转 5 圈，得到稳定的有效 counts/rev。
- 做已知地面距离测量，得到 meter/count。
- 重复往返检查累计回零。

#### MT6701

- 按第 6.4 节做 5～10 轮端点/回中重复性。
- 检查安装松动、止挡弹性和启动零位流程。

#### IMU

- 六面静置确定加速度轴映射和符号。
- 绕车辆 Z 轴左右旋转确定 gyro Z 符号。
- 静置至少 30 秒记录漂移、温度和冻结诊断。
- 验证 3 秒/400 样本零偏标定。

#### 视觉

- 按第 7.6 节记录 SEQ、实际 FPS、PROC 当前/最大值和真实标签数据。

完成标准：所有传感器均有可复现数据表，而不是只显示 `ready=Y`。

### 阶段 2：执行器低功率验收

- 车辆架空，有人准备断电。
- 逐个执行 L+/L-/R+/R-/S+/S-，记录物理方向和对应编码器方向。
- 从低占空比逐步上升，分别测六个起动死区值。
- 转向绝不持续撞机械止挡。
- 修复未标定 CAL 测试的原始计数安全保护。

完成标准：六个方向、六个起动值和执行器停止行为全部记录。

### 阶段 3：修复软件 P0/P1

优先修视觉 stale、相机缓冲所有权、DMA 恢复边沿、硬件 WDT/SMU、CAL 测试保护和启动零位方案。给每项增加可重复故障注入测试。

完成标准：拔相机、停 CPU1、停控制任务、关中断/停止喂狗等测试能按设计进入安全态。

### 阶段 4：完成标定文件

- 填后轮尺度/符号。
- 填电机方向/死区。
- 填转向方向、内缩软限位和两张真实角度表。
- 填 IMU 安装和轴向。
- 填动力学安全边界。
- 保持 `CALIBRATION_VALID=false` 跑主机测试和人工复核。

### 阶段 5：恢复正常应用但保持零动力

```c
#define VEHICLE_TEMP_TEST_MODE (0U)
#define CALIBRATION_VALID      (false)
```

验证：启动、IMU 标定、状态机、UART HELP/CONFIG、CSV、普通 IPS 页面、故障显示，且三路 PWM 始终为零。

### 阶段 6：单闭环调试

在代码审查后启用标定，顺序如下：

1. 转向软限位和低速位置闭环。
2. 左后轮单轮速度环。
3. 右后轮单轮速度环。
4. 双轮直行和低速换向。
5. 轮差、堵转、传感器断开故障注入。

每次只启一个闭环，逐步从很低占空比和很低目标开始。

### 阶段 7：定位验证

- 直线正向/反向已知距离。
- 固定半径左右圆。
- S 弯。
- 比较尺量/外部标记真值和估计误差。
- 调 gyro bias、轮速/转向融合权重和滑移阈值。

### 阶段 8：阶段一路径记录

- 先用串口 DRIVE 低速命令验证，再接视觉。
- 0.05 -> 0.10 -> 0.15 m/s 逐级增加。
- 检查 100 m/120 s 前的停车裕量、点数、路径长度、yaw 和曲率。
- 保存 CSV 和必要的路径导出调试数据。

### 阶段 9：阶段二倒车

- 先短直线，再圆弧，再 S 弯。
- 低于标称速度开始，逐步调 lookahead、反馈和速度曲线。
- 检查索引只减不增、目标速度非正、终点进度门控和受控停车。
- 做定位失效、索引丢失、跟踪误差、轮差等故障注入。

### 阶段 10：发布前验收

- 所有单测、目标 Clean/Build、静态检查通过。
- 每个标定值都有日期、方法、原始数据和负责人。
- 所有 P0 问题关闭。
- 硬件 WDT/SMU 和急停链完成。
- 有完整台架/HIL/封闭场地测试记录。
- 生成正式 Release 标签和对应 ELF/HEX SHA256。

---

## 15. 建议的数据记录模板

交接后不要只发照片，最好每个测试用 CSV/表格保存。最低字段如下。

### 15.1 后轮编码器/电机

```text
日期,轮,raw_DIR,命令占空比,物理方向,编码器delta符号,
有效counts/rev,测量圈数,地面距离m,累计counts,meter/count,
开始转动占空比,电池电压,轮胎压力,备注
```

### 15.2 转向

```text
日期,轮次,接近方向,物理车轮角deg,等效车轮角rad,cnt,
中位误差count,左/右端,命令DIR,命令duty,电池电压,备注
```

### 15.3 IMU

```text
日期,姿态/旋转方向,raw ax,ay,az,gx,gy,gz,temp,
SI ax,ay,az,gx,gy,gz,静置时间,bias,stddev,备注
```

### 15.4 视觉

```text
日期,场景,距离m,横向偏差,标签旋转,照明,
SEQ,FPSx10,PROC_us,PROC_max_us,D,V,P,lost,CONF,
EX,Q15,size,gray10,gray50,gray90,saturation,contrast,误检/漏检
```

---

## 16. 目录和文件职责

### 16.1 关键目录

```text
code/        车辆应用、控制、定位、路径、视觉和测试源码
user/        CPU 入口和中断文件
libraries/   逐飞 V3.4.3、Infineon iLLD 和已修改的 IMU/debug 配置
host_tests/  C99 主机算法测试
Debug/       ADS 生成物；被 Git 忽略，不是源码
tmp/         原理图渲染等临时文件；被 Git 忽略
```

### 16.2 相对 `main` 的新增车辆/视觉文件

```text
code/vehicle_app.c/.h
code/vehicle_calibration.c/.h
code/vehicle_config.h
code/vehicle_control.c/.h
code/vehicle_diagnostics.c/.h
code/vehicle_display.c/.h
code/vehicle_encoder.c/.h
code/vehicle_fault.c/.h
code/vehicle_hal.c/.h
code/vehicle_hardware_config.h
code/vehicle_imu.c/.h
code/vehicle_localization.c/.h
code/vehicle_log.c/.h
code/vehicle_math.c/.h
code/vehicle_mt6701.c/.h
code/vehicle_path.c/.h
code/vehicle_perception.c/.h
code/vehicle_reverse_tracker.c/.h
code/vehicle_safety.c/.h
code/vehicle_state_machine.c/.h
code/vehicle_test_mode.c/.h
code/vehicle_types.h
code/vehicle_vision_camera.c/.h
code/vision_ips_fast.c/.h
code/vision_shared.c/.h
code/vision_tag_tracker.c/.h

host_tests/CMakeLists.txt
host_tests/test_vehicle.c
host_tests/vision_tag_tracker_host_test.c

user/cpu1_main_vision.c
user/isr_vision.c
```

### 16.3 相对 `main` 的已修改文件

```text
.cproject
.gitignore
libraries/zf_common/zf_common_debug.h
libraries/zf_device/zf_device_imu963ra.c
libraries/zf_device/zf_device_imu963ra.h
user/cpu0_main.c
user/isr.c               # 当前被排除，保留模板/兼容修改
```

总差异约为 61 个文件、14639 行新增、25 行删除。不要只复制 `code/`，否则会漏掉 CPU1 入口、视觉 ISR、IMU 软 I2C 修改、UART 波特率和 `.cproject` 排除规则。

### 16.4 外部原始视觉包

视觉运行所需源码已经整合进本工程，没有运行时依赖 Downloads/Temp 中的原始 `SmartCar_TC387_Vision_Tracking` 文件夹。原始包不在本工程 Git 内；如需追溯上游设计，可另行归档，但不能用它覆盖当前已经针对 TC387、多核、DVP 和安全控制修改过的文件。

---

## 17. 交接打包清单

直接交付整个文件夹时确认包含：

- 隐藏 `.git` 目录。
- `.project`、`.cproject`、`.settings`。
- `code`、`user`、`libraries`、`host_tests`。
- 根目录原理图 PDF、推荐 IO 和不推荐引脚资料。
- 当前 `Debug` ELF/HEX/MAP（虽然被 Git 忽略）。
- 本交接文档。

可以不依赖：

- `tmp/pdfs/schematic` 渲染图片；可由 PDF 重建。
- `Debug` 内历史中间目录；新电脑应 Clean/Build。

如果只通过 Git 交接，要额外单独发送当前 ELF/HEX/MAP，因为它们不在 Git 中。

---

## 18. 接手人第一天建议执行的检查单

- [ ] `git branch --show-current` 是 `agent/tc387-vehicle-control`。
- [ ] `git rev-parse HEAD` 是 `5b4b0b137547b767c6366ef8950ad070884b2a93`，或明确知道之后增加了哪些提交。
- [ ] AURIX Studio 1.10.28 / TASKING 1.1r8 能 GUI Clean/Build。
- [ ] 当前 `VEHICLE_TEMP_TEST_MODE=1`，没有误烧正常自动固件。
- [ ] 当前 `CALIBRATION_VALID=false`。
- [ ] 车轮架空，独立断电手段就绪。
- [ ] TEST 1/6 到 5/6 的输入数据记录完成。
- [ ] TEST 6/6 的六个方向只在有人监护时测试。
- [ ] MT6701 5～10 轮重复性完成。
- [ ] 后轮 counts/rev、方向和 meter/count 完成。
- [ ] IMU 轴向/位置完成。
- [ ] 视觉 stale/PCLK/DMA 故障修复完成。
- [ ] 硬件 WDT/SMU/急停方案完成。
- [ ] 全部标定和两张角度表完成后才考虑 `CALIBRATION_VALID=true`。
- [ ] 单闭环、定位、阶段一、阶段二按第 14 节顺序逐项验收。

---

## 19. 最终交接判断

这个工程已经不是空壳：车辆控制、定位、路径记录、倒车、状态机、安全、诊断、日志和固定视觉标签识别的主要软件框架都已经存在，目标 Debug 工程也能生成最新固件，纯算法测试当前全部通过。MT6701 的 TIM5 主计数链已经获得真实台架证据。

但工程目前处于“台架测试与标定阶段”，不是“整车功能完成阶段”。真正剩余工作的核心不是继续堆算法文件，而是：修复已知安全缺陷、取得完整实测标定、逐执行器闭环调试、完成定位和路径实车验证、建立故障注入与硬件安全链。任何人在完成这些步骤前，都不应把测试固件或仅打开标定开关的固件用于有人/有障碍物环境中的自动运行。
